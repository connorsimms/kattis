#include <bits/stdc++.h>
using namespace std;

int main()
{
    cin.tie(nullptr);
    ios_base::sync_with_stdio(false);
    string fen; cin >> fen;

    cout << "AWAWHOO";
    string word = "WOWO";
    for(long long i = 0 ; i <= (long long)fen.size() / 4; ++i) 
    {
	cout << word;
    }
    cout << '\n';
}
